import { FormGroup } from "@angular/forms";

export interface houseInputs{
   area: number, 
   electricCar: boolean, 
   energyHeating: number, 
   energyWater: number, 
   floorHeating: Boolean, 
   insolation: number, 
   kilometersCar: number
}

export interface resitentsInputs{
    
}